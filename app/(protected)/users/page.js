import { UsersContent } from "@/components/users/users-content";

export const metadata = {
  title: "Users — MedClap",
  description: "Manage and review all registered accounts.",
};

export default function UsersPage() {
  return <UsersContent />;
}
