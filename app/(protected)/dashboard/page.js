import { DashboardContent } from "@/components/dashboard/dashboard-content";

export const metadata = {
  title: "Dashboard — MedClap",
};

export default function DashboardPage() {
  return <DashboardContent />;
}
